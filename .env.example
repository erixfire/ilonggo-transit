# IlongGo! Environment Variables
# Copy to .env for local development

# Map tile provider (optional - uses OSM by default)
VITE_MAPBOX_TOKEN=

# Cloudflare Pages / Workers (optional live backend)
VITE_API_BASE_URL=

# Feature flags
VITE_ENABLE_LIVE_TRACKING=false
VITE_ENABLE_ALERTS=true
